A minimal demo workflow: one step serves a static "Hello, world" web page.

Open the entry link at `/hello-world` (restricted to members of this space). The [Hello](<Hello/script.ts>) step writes the full HTTP response itself; there are no downstream steps, connectors, or side effects.
