Serves a single static "Hello, world" HTML page.

Triggered by an HTTP GET on `/hello-world` (space members only). The response is built in [script.ts](script.ts) — no external services, no connectors.
